;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;; YAML mode configuration ;;
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;

;; YAML (YAML Ain't Markup Language) is a human-readable data serialization
;; language. It is commonly used for configuration files, but could be used in
;; many applications where data is being stored (e.g. debugging output) or
;; transmitted (e.g. document headers).
;;
;; Currently ELPA autoloads all the necessary basic configurations, but any
;; additional configurations should be made here.

(provide 'yaml-mode-config)
