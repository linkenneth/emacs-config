;;;;;;;;;;;;;;;;;;;;;;;;;;
;; Semantic Mode Config ;;
;;;;;;;;;;;;;;;;;;;;;;;;;;

;; TODO remove semantic mode in favor of TAGS
;;
;; Semantic mode seems to be able to parse tokens and search by class / method
;; definitions. With helm you can easily search class / method names with
;; g-h-i.

(semantic-mode 1)
