;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;; SCSS mode configuration ;;
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;

;; SCSS is a modified CSS language.

; Don't compile into CSS every time I save
(setq scss-compile-at-save nil)

(provide 'scss-mode-config)
