;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;; HAML mode configuration ;;
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;

;; Haml (HTML Abstraction Markup Language) is a templating system that is
;; designed to avoid writing inline code in a web document and make the HTML
;; easy and clean.
;;
;; Currently ELPA autoloads all the necessary basic configurations, but any
;; additional configurations should be made here.

(provide 'haml-mode-config)
